Implemented and validated.

What satisfies this issue:
- Docs audit command handler now opens/reveals the panel immediately with a loading state.
- Long-running audit work executes in a detached async flow (`void (async () => { ... })`) so command invocation is non-blocking.
- In-flight progress/status updates are posted to the webview while the audit runs.

Source references:
- src/features/doc-auditor/index.ts
  - `panel.webview.html = buildAuditLoadingHtml('Starting…')`
  - `void (async () => { const results = await runAudit(...) ... })`
  - progress updates via `postMessage({ type: 'progress', status: ... })`

Regression coverage added:
- tests/regression/REG-043-doc-audit-non-blocking.test.js
  - verifies immediate loading UI
  - verifies detached async execution path
  - verifies progress/status emission while in flight

Validation run:
- npm run compile
- node tests/regression/REG-043-doc-audit-non-blocking.test.js

All checks passed.
