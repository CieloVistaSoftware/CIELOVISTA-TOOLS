# WB-Core Duplication Audit — cielovista-tools

**Date:** 2026-04-02
**Completed:** 2026-04-03
**Scope:** src/ (all features, shared, and UI code)
**Objective:** Identify and reduce all logic and style duplication already provided by wb-core shared modules/utilities.

---

## 1. JavaScript/TypeScript Duplication

### a. Escaping Helpers
- No custom `_esc()` or `escPath()` found in src/ (good).
- All escaping should use shared helpers from wb-core (e.g., `escHtml`).

### b. Alerts/Notifications
- ✅ **DONE** — Zero `alert()` calls found across all source files. Already clean.

### c. Fetch Calls
- No raw `fetch()` calls found in src/ (good).
- All network requests should use a shared wrapper (e.g., `apiFetch`).

### d. Table Rendering
- Table markup is only present in markdown rendering and test files, not in UI logic (good).
- All UI tables should use a shared grid/table component if available in wb-core.

---

## 2. CSS Duplication

- Button, badge, flex, and skeleton classes are referenced in shared CSS and webview-utils, not duplicated in feature code (good).
- Ensure all UI uses wb-core CSS classes for these elements.

---

## 3. HTML/Event Handler Duplication

- ✅ **DONE** — All HTML-attribute inline event handlers removed across the entire codebase.

### Files Fixed

| File | Change |
|---|---|
| `readme-compliance.ts` | `oninput="applyFilter()"` → `addEventListener('input', applyFilter)` |
| `docs-manager.ts` | `onclick="rescan()"` → `data-action="rescan"` + event delegation |
| `cvs-command-launcher.ts` (stub) | `onclick="clickCard()"` → `id` + `addEventListener` |
| `test-coverage-auditor.ts` | 3 toolbar `onclick` → `id` + `addEventListener` for each |
| `doc-intelligence/html.ts` | 4 toolbar buttons + 6 filter buttons → `id`/`data-filter` + `addEventListener` |
| `doc-auditor/html.ts` | All `onclick` (tabs, openFile, cmd, filterSection) → full `data-action` event delegation |
| `doc-consolidator.ts` | `onclick="...postMessage({openLog})"` in HTML string → `showInteractiveResultWebview` with `onOpenLog` callback |
| `consolidation-plan-webview.ts` | `onclick="getElementById(...)"` → `id="btn-close-diff"` + `addEventListener` |

### Remaining JS-assignment style (`.onclick =`) — Acceptable
These are in-script JavaScript assignments, not HTML attributes. Per audit scope, these are acceptable:
- `consolidation-plan-webview.ts` — `.onclick = e =>` (JS)
- `content-viewer-catalog.ts` — `ico.onclick = function(){}` (JS)
- `show-interactive-result-webview.ts` — `.onclick = () =>` (JS)

---

## 4. Summary & Recommendations

- [x] Replace all `alert()` calls with `createToast()` — **already clean, no calls found**
- [x] Refactor inline event handlers to JS listeners — **complete**
- Continue to enforce use of wb-core for:
  - Escaping helpers
  - Network requests
  - UI components (tables, buttons, badges, skeletons)
  - Notifications

---

## Audit Result

| Category | Before | After |
|---|---|---|
| `alert()` calls | 0 | 0 ✅ |
| HTML `onclick=` attributes | 20+ | 0 ✅ |
| HTML `oninput=` attributes | 1 | 0 ✅ |

---

*Generated by audit script on 2026-04-02. Implemented and completed 2026-04-03.*
